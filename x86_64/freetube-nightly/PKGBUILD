_pkgname=freetube
pkgname=${_pkgname}-nightly
pkgver=0.23.13.build6960.e219ff2
pkgrel=1
pkgdesc="A private YouTube client (nightly build)"
arch=('x86_64')
url="https://github.com/FreeTubeApp/FreeTube"
license=('AGPL-3.0-or-later')
provides=("${_pkgname}")
conflicts=("${_pkgname}")
depends=(
  'c-ares'
  'ffmpeg'
  'gtk3'
  'http-parser'
  'libevent'
  'libvpx'
  'libxslt'
  'libxss'
  'minizip'
  'nss'
  're2'
  'snappy'
  'libnotify'
  'libappindicator-gtk3'
  'ttf-liberation'
)
install=$pkgname.install
source=("https://github.com/jens-buehnen/${pkgname}/releases/latest/download/${_pkgname}.pacman.tar.xz")
sha256sums=('SKIP')

pkgver() {
  curl -s https://api.github.com/repos/jens-buehnen/freetube-nightly/releases/latest | jq -r '.tag_name'
}

package() {
  install -d "$pkgdir/opt/FreeTube"
  cp -r "${srcdir}/opt/" "${pkgdir}/"
  cp -r "${srcdir}/usr/" "${pkgdir}/"
}
